export {
  camelCase,
  capitalize,
  escape,
  kebabCase,
  lowerCase,
  pad,
  padEnd as padr,
  padStart as padl,
  snakeCase,
  toLower,
  toUpper,
  trim,
  trimEnd as trimr,
  trimStart as triml,
  unescape,
  upperCase
} from 'lodash';

export const str = (...args) => args.join('');
